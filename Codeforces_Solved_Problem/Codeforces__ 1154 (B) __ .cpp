#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n,a[110],di[110];

    while(cin>>n)
    {
        for(int i=0; i<n; i++)
            cin>>a[i];

        sort(a, a+n);


    }
    return 0;
}
