#include<bits/stdc++.h>
using namespace std;

#define SIZE 3010

int main()
{
    int voter,parties,pref[SIZE],cost[SIZE],num[SIZE];

    while(scanf("%d %d", &voter, &parties) != EOF)
    {
        memset(num,0,sizeof(num));
        int maxi = INT_MIN;
        for(int i=1; i<=voter; i++)
        {
            scanf("%d %d", &pref[i], &cost[i]);
            num[pref[i]++;
            if(num[pref[i] >)
        }

    }
    return 0;
}
